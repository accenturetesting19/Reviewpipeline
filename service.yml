apiVersion: v1
kind: Service
metadata:
  name: testapp-service
spec:
  selector:
    app: testappdeploy 
  type: LoadBalancer
  ports:
  - protocol: TCP
    port: 3000
    targetPort: 4000
    nodePort: 31111